log {

    level    = "warning" # Options: fatal, critical, error, warning, info, debug, trace
    console  = on
    filename = "hearbeat-server.log"
    roll     = "never"  # Options: never, hourly, daily, sunday/monday/...

    level-akka {
        node  = "org.sherman.heartbeat.server"
        level = "warning"
    }
}

akka {}